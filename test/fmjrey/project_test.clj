(ns fmjrey.project-test
  (:require [clojure.test :refer [deftest is testing]]
            [fmjrey.project :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
